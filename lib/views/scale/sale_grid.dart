import 'package:flutter/material.dart';

class SaleGrid extends StatelessWidget {
  const SaleGrid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        color: Colors.white,
      ),
    );
  }
}
